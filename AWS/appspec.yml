version: 0.0
os: linux

hooks:
  ApplicationStop:
    - location: AWS/stop_container.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: AWS/start_container.sh
      timeout: 300
      runas: root
